import Foundation

enum Constants {
    static let appName = "Coffee-Screen"
    static let emergencyKeyCombo = "Shift+Shift+Cmd+L"
    static let shieldWindowLevel = 1001

    enum Strings {
        static let lockButton = String(localized: "lock.button")
        static let unlockButton = String(localized: "unlock.button")
        static let unlockReason = String(localized: "unlock.reason")
        static let authFailed = String(localized: "error.auth.failed")
        static let powerWarning = String(localized: "warning.power")
    }
}
